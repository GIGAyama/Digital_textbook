# note 記事の入稿メモ

連載「教室で使えるかもしれないもの作り」に載せる、デジタル教科書メーカーの紹介記事一式です。

```
docs/note/
├── digital-textbook-note-article.md   記事本文。そのまま note に貼れる形
├── images/                            実際に操作して撮った画面（23点）
└── README.md                          このファイル
```

---

## 公開の前に、これだけ先にお願いします

**この記事より先に、コミット `c7f4409` を本番へ反映してください。**

撮影のためにアプリを一通り操作していて、ツールバーの2つのメニューが画面の右外へはみ出すのを見つけました。記事とは別のコミットに分けてあるので、修正だけ先に出せます。

- スタンプのパネルが、画面幅 1280px で 90px、1366px で 47px はみ出していました。英語と理社のタブ、4列目のスタンプが押せません。**1366x768 は学校の Chromebook でいちばん多い解像度です。**
- 「表示」のメニューは、ツールバーのいちばん右のボタンなのに左端に合わせて開くため、「ページ全体を表示」の文字が切れていました。

どちらもボタンの右端に合わせて左へ開くように直し、1024 / 1280 / 1366 / 1440 / 1600 / 1920px の6つの幅で、はみ出しがゼロになることを実ブラウザで測って確認しています。記事の画像（`09-stamp-eval.png` など）は修正後の画面なので、**修正を反映しないまま記事を出すと、画像と本番が食いちがいます。**

---

## 貼るまえに直すところ

| 場所 | いまの状態 | やること |
|---|---|---|
| タイトル行 | `#◯` | 連載の実際の番号に置きかえてください。推測で埋めていません |
| タイトル | 「このプリント、そのまま大きく映したい」PDFに直接書きこめるアプリ「デジタル教科書メーカー」 | 長ければ短くしてください。型は `「困りごと」＋持ち味＋アプリ「名前」` です |
| まとめの最後 | 「作った本人が現場にいるので、次の日には直せることもあります」 | 実情に合わなければ削ってください |

---

## note への貼りかた

1. 記事本文の1行目（`# 教室で…`）を、note のタイトル欄に貼ります。本文には入れません。
2. 2行目以降を本文欄に貼ります。`##` の見出しはそのまま見出しとして認識され、note が目次を自動で作ります。
3. 画像は自動では入りません。`![...](images/xx-....png)` の行のところで、下の対応表を見ながら手動でアップロードしてください。
4. キャプション（画像の下の一行）は、貼りつけたあと note のキャプション欄へ移してください。本文に残したままでも読めますが、キャプション欄に入れたほうが見た目が整います。
5. ハッシュタグは `#GIGAスクール` `#ICT教育` `#小学校` `#教員` `#デジタル教科書` あたりが合うと思います。
6. 公開前に、アプリの URL が生きているかを一度確認してください。

---

## 画像の対応表

すべて実ブラウザ（Chromium）を操作して撮ったものです。合成・加工はしていません。表示倍率は2倍で撮っているので、note で拡大されても粗くなりません。

| ファイル | 何の画面か | 本文のどこ |
|---|---|---|
| `01-home.png` | 開いた直後のホーム。まだ何も入っていない | 📱 このアプリでできること |
| `02-library.png` | PDF を2冊読みこんだあとの棚 | 📱 |
| `03-textbook.png` | プリントを開いた直後。道具は隠れている | 📱 |
| `04-toolbar.png` | 「ツール」を押してツールバーを出したところ | 📱 |
| `05-pen.png` | 赤いえんぴつで丸つけと直しの線 | 📱 |
| `06-marker.png` | もんだい文にマーカー | 📱 |
| `07-sticky.png` | 黄色いふせんに注意点を書いて貼ったところ | 📱 |
| `08-marked.png` | 丸つけ・マーカー・ふせん・はなまるを全部のせたページ | ✍️ 丸つけの道具 |
| `09-stamp-eval.png` | スタンプの棚（評価タブ） | ✍️ |
| `10-stamp-math.png` | スタンプの棚（算数タブ） | ✍️ |
| `11-stamp-jp.png` | スタンプの棚（国語タブ） | ✍️ |
| `12-math-tools.png` | 筆算のわく・位取り表・三角定規・立方体を置いたページ | ✍️ |
| `13-mystamp.png` | マイスタンプ作成のダイアログ | ✍️ |
| `14-view-menu.png` | 「表示」のメニュー | 📱 |
| `15-half.png` | 半ページ表示。タブレット縦画面（820x1180）で撮影 | 📱 |
| `16-presentation.png` | 提示モード | 📱 |
| `17-timer.png` | タイマー | 📱 |
| `18-shortcuts.png` | ショートカット一覧 | 📱 |
| `19-share.png` | 共有の画面。QRコードとアドレス | 📤 配れます |
| `20-received.png` | 子どもの端末で開いたところ。書きこみごと届いている | 📤 |
| `21-kid-library.png` | 子どもの棚。題名に「(共有)」が付く | 📤 |
| `22-jp-vertical.png` | 国語の説明文に縦書きスタンプとマーカー | ✍️ |
| `23-backup.png` | バックアップ取り込みの確認画面 | 🛠️ 導入手順 |

### 画像について、断っておくこと

- **サンプルの PDF は撮影用に作った架空のプリントです。** 実在の教科書・ワークではありません。3年算数「あまりのある わり算」4ページと、4年国語「だんらくの つながり」2ページを、撮影のためにその場で作りました。
- **共有画面（`19-share.png`）の URL は `http://` で写っています。** 本番は `https://gigayama.github.io/Digital_textbook/` です。撮影中だけ本番のホスト名を手もとのサーバーに向けており、そこだけ本番と見た目が異なります。気になる場合は、実機で撮り直してください。
- **「データを受信しています」のモーダルは撮れませんでした。** 手もとの環境では受信が速すぎて（1秒未満）、モーダルが表示される前に完了してしまいます。実際の校内ネットワークでは表示されるはずなので、必要なら実機で撮り足してください。20番はその直後の「受け取り終わった画面」です。
- 子どもの端末の画面（`20`, `21`）は、先生の端末とは別のブラウザプロファイルを立ち上げて、本当に2台つないで撮ったものです。合成ではありません。

---

## 記事に書いた数字の出どころ

| 数字 | 出どころ |
|---|---|
| スタンプ 167種類 | `src/data/stamps.js` を数えた結果と、アプリを開いてスタンプの棚のボタンを数えた結果が一致（評価16／国語41／算数42／英語10／理社34／生活24） |
| 評価16・算数42・国語41・英語10・理社34・生活24 | 同上。アプリ上で実測 |
| ふせんの色 4色 | `src/constants.js` の `STICKY_COLORS` |
| ペンの色 5色 | `src/constants.js` の `COLORS` |
| 一度に配るのは10人ずつ | `README.md` の「制限とクォータ」。実測はしていないため、記事では「10人ずつくらいに分けたほうがうまくいきます」と幅を持たせた書きかたにしています |
| 300ページを超えると止まることがある | `README.md` の「制限とクォータ」。メモリ4GBの Chromebook での挙動。実測はしていません |
| iOS Safari の7日 | `MANUAL.md` と `README.md`。Safari の ITP の仕様 |
| 通信を許可するアドレス | `index.html` の Content-Security-Policy をそのまま読み下したもの。`gigayama.github.io` / `fonts.googleapis.com` / `fonts.gstatic.com` / `0.peerjs.com` / `accounts.google.com` / `www.googleapis.com` |
| Googleドライブが触れるのは自分の作ったファイルだけ | `src/constants.js` の `GDRIVE_SCOPE` が `drive.file` であること |
| 「(共有)」が題名に付く | `src/App.jsx` の受信処理。`21-kid-library.png` で実際に確認 |

### 記事に書かなかった数字

- **読み込み時間。** 手もとの開発機では4ページで約0.7秒、2ページで約0.6秒でしたが、これは Chromebook での体感とはかけ離れます。誤解を招くので記事には入れず、「ページ数によります」という書きかたにとどめました。
- **「リアルな分度器」。** リポジトリの `README.md` には「リアルな分度器・三角定規」とありますが、実際に置いてみると、分度器は `⌒` の文字、三角定規は直角三角形の線画でした。記事では「三角定規」とだけ書き、分度器には触れていません。**リポジトリの README のほうを直すか、スタンプの絵を実物に寄せるか、どちらかをおすすめします。**

---

## 撮り直すときの手順

撮影に使ったスクリプトは `.tmp-shots/` に置いて作業し、納品時に消しました。同じものを作り直す場合の手順です。

1. 撮影の道具をまとめて入れます。`npm i --no-save playwright peer`
2. 本番と同じ見た目にするため、本番のホスト名を手もとに向けます。`echo "127.0.0.1 gigayama.github.io" >> /etc/hosts`
3. 共有機能を撮るには、待ち合わせ用のサーバーを手もとに立てます。`node <skill>/scripts/peer-server.mjs 9000`
4. アプリ側の接続先を一時的に差しかえます。`src/App.jsx` の `new window.Peer()` を2か所とも `new window.Peer(undefined, { host: '127.0.0.1', port: 9000, path: '/', secure: false })` にして、`index.html` の CSP の `connect-src` に `http://127.0.0.1:9000` と `ws://127.0.0.1:9000` を足します。
5. `npm run build` して、`node <skill>/scripts/serve.mjs dist 80 /Digital_textbook/` で配ります。
6. `node <skill>/scripts/capture.mjs shots.mjs --base http://gigayama.github.io/Digital_textbook/ --out shots` で撮ります。
7. **4の差しかえを必ず戻して、`git status` と `git diff` で差分に残っていないか確かめてください。** ここを戻し忘れると本番の共有機能が壊れます。2の `/etc/hosts` も消します。

撮影のシナリオで手こずったところを2つだけ書き残しておきます。ツールバーのアイコンだけのボタンは `title` 属性しか持たないので、文字では押せません（`button[title="えんぴつ"]` のように指定します）。それから、置いたスタンプの位置と大きさは、fabric.js の中身に触れずに知る必要があるので、置く前と置いた後のキャンバスの画素を見くらべて求めました。スタンプは中心を軸に拡大縮小されるため、角のつまみは中心からの距離で計算します。

---

## 文体について踏襲したこと

`references/style.md` の指定に沿って書いています。機械的に確かめられるところは確認済みです。

- 太字 0か所、表 0行、禁止記号（`→` `／` `▲` `▶` `：`）0か所
- 本文 7,789字（画像の記法を除く。目安の7,000〜9,000字の範囲内）
- 絵文字は見出しのみ。本文には1つも入っていません
- 箇条書きは「導入手順」と「使い方のガイド」の手順だけ。それ以外は文章で書いています
- 見出しは基本の6本に、目玉のスタンプと共有の2本を足した8本。追加は2本までという上限どおりです
- 専門用語は言いかえています。WebRTC は「端末から端末へ直接」、IndexedDB は「その端末のなか」、OAuth スコープは「このアプリが作ったファイルだけ」としました
- 「使ってみたら子どもたちが〜」といった実践記録は一行も書いていません。作者の見立てとして「〜だと思います」「〜ではないかと思います」でとどめています
- 効果は言い切っていません。「浮いた時間で、赤ペンでひとこと書く余裕が生まれるかもしれません」のように、可能性の書きかたにしています
- 結びは #9 と同じ「一歩踏み出そうとする先生を、私はいつでも全力で応援しています。」です
